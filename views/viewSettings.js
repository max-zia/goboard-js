var startx = 50;                // top-left corner of board (x coordinate)
var starty = 50;                // top-right corner of board (y coordinate)
var gap = 25;                   // gap between intersections

var intersectionR = 1;          // radius of intersection markers (id="p")
var stoneR = 10;                // radius of playing stone (id="p-stone")
